ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

database:
    jdbcUrl: ${?DB_URL}
    driverClassName: ${?DB_DRIVER}
    username: ${?DB_USER}
    password: ${?DB_PASSWORD}
